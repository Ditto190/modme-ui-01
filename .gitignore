# Secrets and local env
.env
.env.*
!.env.example
.orca/
.orca.json
orca.json

# Local data and sandbox persistence (never commit client data)
data/
menv/

# Dependencies and package managers
node_modules/
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/sdks
!.yarn/versions

# Build outputs
dist/
build/
out/
.cache/builders/
.cache/control-cli/
.next/
.turbo/
coverage/
target/
*.tsbuildinfo

# Worktree local artifacts (git manages worktree dirs themselves)
.worktrees/**/.worktree-ports.env
.worktrees/**/logs/
.lean-ctx/
.node/
.firecrawl/

# Vendored mirrors (re-fetch via scripts)
.vendor/
# Firecrawl self-host clone: yarn firecrawl:setup → .vendor/firecrawl/

# Logs and diagnostics
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
# Session logger logs (local-only)
logs/copilot/
logs/agent-orchestrator/
logs/lean-ctx/

# Agent task registry (local JSON)
data/
!data/lean-ctx-task-profiles.toml.example

# Copilot/session exports (local only)
session.md

# Generated mprocs config (per-worktree ports)
mprocs.yaml

# OS and IDE
.DS_Store
Thumbs.db
*.swp
*.swo
.idea/
*.iml

# Cursor local / ephemeral
.cursor/**/*.log
.cursor/**/mcp.json
.cursor/hook-logs/

# UniversalWorkbench build artifacts
**/UniversalWorkbench-staging/**/dist/
**/UniversalWorkbench-dev/**/dist/
**/UniversalWorkbench/**/dist/
**/UniversalWorkbench/**/.next/
**/UniversalWorkbench/**/node_modules/

# Test and temp
.tmp/
temp/
docs/devops/reports/
**/playwright-report/
**/test-results/
**/intake-pipeline/outgoing/
**/debug-test-output.json
# Inbox is a local drafts funnel — keep content private but commit the manifest and README
**/docs/inbox/*
!**/docs/inbox/README.md
!**/docs/inbox/_index.json
.agents/temp_skills/
*.bak

# Preflight / DevOps reports (generated locally and in CI)
docs/devops/reports/

**/.voltagent/*.db
# lean-ctx machine-local id (project config .lean-ctx.toml is tracked)
.lean-ctx-id

# agenttrace
.agenttrace/
/agenttrace-report.*
/agenttrace-baseline.*

# Local Cursor hook runtime state (not project config)
.cursor/hooks/state/

# IDE-local skill/runtime dirs (use .cursor/skills and .agents/skills in repo)
.claude/
.copilot/

# Misc local artifacts
quality/
1.1.0
-w
*.backup
.agents/skills/.antigravity-install-manifest.json
.agents/

# Beads / Dolt files (added by bd init)
.dolt/
.dolt-data/
.beads/embeddeddolt/
.beads/interactions.jsonl
.beads/backup-pre-*.jsonl
.beads/backup/
.beads/backup-store/
*.db
.beads-credential-key
.beads/proxieddb/

# Entire local overrides (settings.json may be committed)
.entire/settings.local.json
.entire/tmp/
.entire/logs/
.entire/metadata/
.entire/current_session

/generated/prisma