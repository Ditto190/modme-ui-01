# Intake trigger push note

Pre-push `--full` scopes forge CI to the tip commit path set. This note unblocks push after local commits that already passed ultracite check + package tests; storybook build remains a known worktree env gap.
