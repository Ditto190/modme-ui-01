# Phoenix + OpenInference Dependencies
# Install with: pip install -r requirements-phoenix.txt

# Core Phoenix
arize-phoenix>=4.0.0

# OpenInference Instrumentation
openinference-instrumentation-anthropic>=0.1.0
openinference-instrumentation-openai>=0.1.0
# Note: Google instrumentation not yet available for Python 3.12+
# openinference-instrumentation-google-generativeai>=0.1.0

# OpenTelemetry (if not already installed)
opentelemetry-api>=1.20.0
opentelemetry-sdk>=1.20.0
opentelemetry-exporter-otlp-proto-http>=1.20.0

# FastAPI for telemetry proxy server
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
pydantic>=2.0.0

# HTTP client for Phoenix API queries
requests>=2.31.0

# Data export dependencies (optional)
pandas>=2.0.0  # For CSV export
pyarrow>=14.0.0  # For Parquet export

# Optional: PostgreSQL support for production
# psycopg2-binary>=2.9.0
# sqlalchemy>=2.0.0
