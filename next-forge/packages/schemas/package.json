{
  "name": "@repo/schemas",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "clean": "git clean -xdf .cache .turbo dist node_modules",
    "typecheck": "tsc --noEmit --emitDeclarationOnly false",
    "test": "vitest run --passWithNoTests",
    "export:inbox-contract": "node scripts/export-inbox-contract.mjs"
  },
  "dependencies": {
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@repo/typescript-config": "workspace:*",
    "@types/node": "25.3.5",
    "typescript": "^5.9.3",
    "vitest": "^4.0.18"
  }
}
