# mise — agent/CI toolchain pins (parity for review agents)
# https://mise.jdx.dev/
# Root Nix/devbox.json is intentionally out of scope; use mise for CLIs.

[tools]
node = "20"
bun = "1.3.10"
gh = "2.74.2"

[settings]
experimental = true
