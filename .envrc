# ModMe repo environment (WSL / devcontainer / Git Bash with direnv)
dotenv_if_exists
watch_file devbox.json
watch_file package.json

if has devbox; then
  eval "$(devbox shellenv)"
fi

export MODME_REPO_ROOT="$(pwd)"
PATH_add scripts
