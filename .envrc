# Monorepo_ModMe — auto-loaded by direnv when you enter this directory.
# Docs: https://direnv.net/man/direnv-stdlib.1.html
# Allow once: direnv allow

# Load root .env if present (never commit secrets)
dotenv_if_exists

# Repo root for scripts and agents
export MONOREPO_MODME_ROOT=$PWD
