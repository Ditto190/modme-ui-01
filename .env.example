# ==================================
# ModMe GenUI Workspace Environment
# ==================================

# Google AI (Required for ADK Agent)
# Get your API key from: https://makersuite.google.com/app/apikey
GOOGLE_API_KEY=your_google_api_key_here

# Application Settings
NODE_ENV=development
PORT=3000
AGENT_PORT=8000

# Python Settings
PYTHONPATH=./agent

# Logging
LOG_LEVEL=info
# Options: debug, info, warn, error

# CopilotKit Configuration
COPILOT_CLOUD_API_KEY=
# Optional: Only needed if using CopilotKit Cloud features

# Database (if applicable)
# DATABASE_URL=

# Privacy & Local-First Settings
# Set to 'true' to enforce local-only mode (no external API calls except configured ones)
LOCAL_ONLY_MODE=false

# Data Directory
DATA_DIR=./data

# Agent Configuration
AGENT_MODEL=gemini-2.5-flash
# Options: gemini-2.5-flash, gemini-pro, etc.

AGENT_TIMEOUT=30
# Timeout in seconds for agent responses

# VT Code MCP Integration
VTCODE_MCP_URL=http://localhost:8080
VTCODE_ENABLED=true
VTCODE_TIMEOUT_SECONDS=30

# Development Tools
ENABLE_DEBUG_MODE=false
ENABLE_HOT_RELOAD=true

# GitHub Actions (for CI/CD)
# These are typically set automatically in CI environments
# GITHUB_TOKEN=
# GITHUB_REPOSITORY=
# GITHUB_WORKSPACE=

# MCP Server Configuration (if applicable)
# MCP_SERVER_URL=http://localhost:8080
# MCP_SERVER_API_KEY=

# Workspace Type
WORKSPACE_TYPE=genui-devcontainer

# ========================================
# Semantic Router Configuration
# ========================================

# Routing Mode: "local" (privacy-first, HuggingFace) or "cloud" (OpenAI)
SEMANTIC_ROUTER_MODE=local

# Cloud mode only - OpenAI API key for embeddings
# OPENAI_API_KEY=sk-...

# Routing Thresholds
SEMANTIC_ROUTE_THRESHOLD=0.5
ENSEMBLE_TOP_K=3

# Feature Flags
ENABLE_SEMANTIC_ROUTING=true
ENABLE_ROUTE_LOGGING=true
ENABLE_ENSEMBLE_MODE=true
