// This file is auto-generated by @hey-api/openapi-ts

import { client } from './client.gen.js';
import type { Client, Options as Options2, TDataShape } from './client/index.js';
import type { HealthCheckData, HealthCheckResponses, IngestChatTracesData, IngestChatTracesErrors, IngestChatTracesResponses, ListSupportedFormatsData, ListSupportedFormatsResponses } from './types.gen.js';

export type Options<TData extends TDataShape = TDataShape, ThrowOnError extends boolean = boolean> = Options2<TData, ThrowOnError> & {
    /**
     * You can provide a client instance returned by `createClient()` instead of
     * individual options. This might be also useful if you want to implement a
     * custom client.
     */
    client?: Client;
    /**
     * You can pass arbitrary values through the `meta` object. This can be
     * used to access values that aren't defined as part of the SDK function.
     */
    meta?: Record<string, unknown>;
};

/**
 * Ingest normalized chat traces into Phoenix
 *
 * Accepts pre-normalized conversation turns in the Universal Turn Format and
 * uploads them as OpenInference-compliant OTLP traces to Phoenix.
 *
 * The payload should be normalized by the n8n workflow before calling this endpoint.
 * Each turn becomes an AGENT → LLM span (with optional TOOL child spans).
 */
export const ingestChatTraces = <ThrowOnError extends boolean = false>(options: Options<IngestChatTracesData, ThrowOnError>) => (options.client ?? client).post<IngestChatTracesResponses, IngestChatTracesErrors, ThrowOnError>({
    url: '/ingest',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * List supported chat formats
 *
 * Returns the list of chat formats the bridge can process.
 */
export const listSupportedFormats = <ThrowOnError extends boolean = false>(options?: Options<ListSupportedFormatsData, ThrowOnError>) => (options?.client ?? client).get<ListSupportedFormatsResponses, unknown, ThrowOnError>({ url: '/formats', ...options });

/**
 * Health check
 */
export const healthCheck = <ThrowOnError extends boolean = false>(options?: Options<HealthCheckData, ThrowOnError>) => (options?.client ?? client).get<HealthCheckResponses, unknown, ThrowOnError>({ url: '/health', ...options });
