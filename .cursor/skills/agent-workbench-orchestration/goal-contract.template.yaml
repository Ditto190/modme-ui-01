# Copy and fill before launching the agent workbench orchestration workflow.
task_id: ""
title: ""
acceptance:
  - "WebSocket emits token, tool_start, tool_result, done events"
  - "useAgentState reconciles optimistic messages on server ack"
  - "Glass skeleton visible during idle/processing/streaming"
  - "StreamingText animates token deltas with reduced-motion fallback"
  - "visual-qa-testing passes (console clean, network healthy)"
metrics:
  max_tokens_orchestrator: 8000
  max_tokens_per_subagent: 12000
  parallel_lanes: 2
  max_grind_cycles: 5
agents:
  coordinator: llm-app-patterns
  backend: cc-skill-backend-patterns
  frontend_logic: react-patterns
  frontend_design: frontend-design
  meta: awesome-cursor-skills
  gate: code-reviewer
rollback: "Revert BE+FE commits if visual-qa or review fails twice"
