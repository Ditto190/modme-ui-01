This repository's `main` branch was created from `feature/genui-workbench-refactor` to bring integration changes from the codespace.

Next steps:
- Review CI checks triggered by the PR from `feature/genui-workbench-refactor` into `main`.
- Validate security alerts and dependency issues listed on the repository's Security page.
- If acceptable, merge the PR and protect `main` as needed.
